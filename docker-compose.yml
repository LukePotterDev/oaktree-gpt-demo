services:
  oaktree-gpt-demo:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  oaktree-gpt-web:
    build: ./web
    ports:
      - "5050:5000"
    environment:
      - PYTHONUNBUFFERED=1
      - API_BASE_URL=http://oaktree-gpt-demo:8000
      - API_KEY=demo-key-2026
    depends_on:
      - oaktree-gpt-demo
    restart: unless-stopped
